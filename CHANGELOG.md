# Changelog

## Unreleased

- Add an opt-in workspace/spec-only mode that preserves governance and Spec Hub sync while skipping generated and curated collection assets, and discard tracked asset IDs when onboarding targets a different workspace.
